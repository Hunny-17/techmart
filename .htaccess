# Chuyển hướng mọi request vào thư mục public/
# Document root nên trỏ thẳng vào public/ khi deploy
# File này dùng để khi cài trên localhost không cấu hình được document root

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
